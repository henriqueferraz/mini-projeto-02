# Copie este arquivo para .env e preencha apenas localmente.
# Nunca versionar o arquivo .env (já listado no .gitignore).
#
# OPENAI_API_KEY vazio → análise em modo heuristic
# OPENAI_API_KEY preenchido → análise em modo llm (via langchain-openai)

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
